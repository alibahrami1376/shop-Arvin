[tool.ruff]
target-version = "py312"
line-length = 88
exclude = [
  ".venv",
  "venv",
  "migrations",
  "static",
  "public",
  "media",
  "postgres",
]

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.ruff.format]
quote-style = "double"